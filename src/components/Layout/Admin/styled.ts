import { Breadcrumb, Layout, Menu } from "antd";
import Sider from "antd/es/layout/Sider";
import { Content, Footer, Header } from "antd/es/layout/layout";
import { styled } from "styled-components";

export const CLayout = styled(Layout)``

export const CSider = styled(Sider)``

export const CMenu = styled(Menu)``

export const CHeader = styled(Header)``

export const CContent = styled(Content)``

export const CBreadcrumb = styled(Breadcrumb)``

export const CBreadcrumbItem = styled(Breadcrumb.Item)``

export const CFooter = styled(Footer)``